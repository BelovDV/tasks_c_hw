#include "disassembler.h"

int disassemble(const Array_static_frame *bin, FILE *dest, FILE *log)
{
	fprintf(log, "Disassembler began working\n");
	fprintf(log, "Disassembler ended working\n");
	return 0;
	(void)bin;
	(void)dest;
}